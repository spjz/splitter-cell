import { writable } from 'svelte/store';

export const animationSpeed = writable(1);
export const autoSplitRate = writable(1);
export const autoSplitEnabled = writable(true);
export const controlsVisible = writable(true);
